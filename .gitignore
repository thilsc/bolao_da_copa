Nothing needs to be added to .gitignore since the only change is adding a text file (`backend/matches_data_official.txt`) which appears to be source data rather than a build artifact, dependency, or temporary file.